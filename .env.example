# Supabase Configuration
# These variables are safe to expose in client-side builds
# The ANON key is designed to be public and is protected by Row Level Security (RLS)

VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Webhooks
VITE_OPPORTUNITY_CREATED_WEBHOOK_URL=your_opportunity_created_webhook_url